name: Build, Test and Deploy

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: db_raiffeisen
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h localhost --silent"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=3
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4.2.2

      # Setup Java and Maven for Server Tests
      - name: Setup Java
        uses: actions/setup-java@v4.6.0
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      # Wait for MySQL service to be ready
      - name: Wait for MySQL
        run: |
          for i in {1..10}; do
            if mysqladmin ping -h 127.0.0.1 --silent; then
              echo "MySQL is up!"
              break
            fi
            echo "Waiting for MySQL..."
            sleep 5
          done

      # Check Database Connection
      - name: Check Database Connection
        run: |
          echo "Trying to connect to MySQL..."
          mysql -h 127.0.0.1 -u root -proot -e "SHOW DATABASES;"

      # Server: Install Dependencies and Run Server Tests
      - name: Install Dependencies und Run Tests (Server)
        env:
          JWT_SECRET: ${{ secrets.SECRET_KEY }}
        run: |
          cd ..
          cd Raiffeisen_Onboarding
          mvn clean install
          mvn test

      # Collect Server Test Reports
      - name: Collect Server Test Reports
        run: |
          mkdir -p server-test-results
          cp target/surefire-reports/*.xml server-test-results/

      # Client: Install Dependencies and Fix Vulnerabilities
      - name: Install Dependencies und Fix Vulnerabilities (Client)
        run: |
          cd Onboarding_client
          npm install
          npm audit fix || true

      # Client: Update Angular and Dependencies
      - name: Update Angular and Dependencies
        run: |
          cd Onboarding_client
          npm install -g @angular/cli@latest
          ng update @angular/core @angular/cli || true
          npm fund

      # Client: Run Angular Tests
      - name: Run Angular Tests
        run: |
          cd Onboarding_client
          npm test -- --watch=false --browsers=ChromeHeadless || true

      # Collect Client Test Reports
      - name: Collect Client Test Reports
        run: |
          ls
          mkdir -p client-test-results
          cp Onboarding_client/test-results/*.xml client-test-results/

      # Add E2E Tests with Cypress
      - name: Install Cypress and Run E2E Tests
        run: |
          cd Onboarding_client
          npm install cypress --save-dev  # Install Cypress
          npx cypress run --headless --no-exit  # Run E2E tests in headless mode

      # Collect Cypress Test Reports
      - name: Collect Cypress Test Reports
        run: |
          mkdir -p cypress-test-results
          cp Onboarding_client/cypress/reports/* cypress-test-results/
#
      # Upload all Test Reports
      - name: Upload Test Reports
        uses: actions/upload-artifact@v4.5.0
        with:
          name: test-reports
          path: |
            server-test-results/
            client-test-results/
            cypress-test-results/

      # List Docker Images (optional, for debugging)
      - name: List Docker Images
        run: docker images
