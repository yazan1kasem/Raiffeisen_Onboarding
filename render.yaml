services:
  - name: db_raiffeisen
    type: pserv
    plan: free
    envVars:
      - key: MYSQL_DATABASE
        value: db_raiffeisen
      - key: MYSQL_ROOT_PASSWORD
        value: root
    image: mysql:8
    ports:
      - "3306"
    disk:
      name: db-storage
      mountPath: /var/lib/mysql

  - name: onboarding-frontend
    type: web
    runtime: docker
    repo: https://github.com/yazan1kasem/Raiffeisen_Onboarding.git/Onboarding_client
    branch: master
    plan: starter
    envVars:
      - key: SERVER_PORT
        value: "4200"

  - name: onboarding-backend
    type: web
    runtime: docker
    repo: https://github.com/yazan1kasem/Raiffeisen_Onboarding.git
    branch: master
    plan: starter
    envVars:
      - key: SPRING_DATASOURCE_URL
        value: jdbc:mysql://db_raiffeisen:3306/db_raiffeisen?serverTimezone=UTC
      - key: SPRING_DATASOURCE_USERNAME
        value: root
      - key: SPRING_DATASOURCE_PASSWORD
        value: root
      - key: SERVER_PORT
        value: "8081"
      - key: SECURITY_JWT_SECRET_KEY
        sync: false
      - key: SECURITY_JWT_EXPIRATION_TIME
        value: "36000000"
    healthCheckPath: /actuator/health
    region: frankfurt
    dockerfilePath: Dockerfile
    dependsOn:
      - name: db_raiffeisen
        condition: service_healthy
