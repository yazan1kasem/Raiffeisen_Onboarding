services:
  - name: onboarding-frontend
    type: web
    runtime: static
    repo: https://github.com/yazan1kasem/Raiffeisen_Onboarding.git
    branch: master
    buildCommand: cd Onboarding_client && npm install && npm run build
    staticPublishPath: Onboarding_client/dist/Onboarding_client
    envVars:
      - key: API_URL
        value: https://onboarding-backend.onrender.com
    routes:
      - source: /.*
        destination: /index.html
        type: redirect
  - name: onboarding-backend
    type: web
    runtime: docker
    repo: https://github.com/yazan1kasem/Raiffeisen_Onboarding.git
    branch: master
    plan: starter
    envVars:
      - key: SPRING_DATASOURCE_URL
        fromDatabase:
          name: db_raiffeisen
          property: connectionString
      - key: SPRING_DATASOURCE_USERNAME
        fromDatabase:
          name: db_raiffeisen
          property: user
      - key: SPRING_DATASOURCE_PASSWORD
        fromDatabase:
          name: db_raiffeisen
          property: password
      - key: SPRING_JPA_HIBERNATE_DDL_AUTO
        value: update
      - key: SERVER_PORT
        value: "8081"
      - key: SECURITY_JWT_SECRET_KEY
        sync: false  # 🔒 Secret bleibt sicher
      - key: SECURITY_JWT_EXPIRATION_TIME
        value: "36000000"
    healthCheckPath: /actuator/health
    region: frankfurt
    dockerfilePath: Dockerfile

  - name: db_raiffeisen
    type: pserv  # ✅ Erlaubter Typ für persistente Services
    plan: starter
    envVars:
      - key: MYSQL_DATABASE
        value: db_raiffeisen
      - key: MYSQL_USER
        value: root  # 🔐 Sicherer Benutzername
      - key: MYSQL_PASSWORD
        value: root  # 🔒 Sicheres Passwort wird generiert
      - key: MYSQL_ROOT_PASSWORD
        value: root  # 🔒 Sicheres Root-Passwort wird generiert
    disk:
      name: db_raiffeisen
      mountPath: /var/lib/mysql
      sizeGB: 10
    repo: https://github.com/render-examples/mysql
    region: frankfurt
    branch: oh/mysql_8.0.24
    runtime: docker
